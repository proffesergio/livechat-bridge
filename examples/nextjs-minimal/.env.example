# Optional: enable real Claude replies instead of the local fake AI provider.
# Leave unset for an offline demo.
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the fallback wait. Default is 30000 ms (30 s).
# AI_FALLBACK_MS=8000
